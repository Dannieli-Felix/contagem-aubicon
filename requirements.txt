# Versões fixadas para o servidor reproduzir o ambiente validado.
# A leitura de geometria do PDF depende da versão do PyMuPDF — manter fixa.
streamlit>=1.40
PyMuPDF==1.26.5
shapely==2.0.7
numpy>=1.26,<3
