# Versões fixadas para o servidor reproduzir EXATAMENTE o ambiente validado.
# (A leitura do PDF e a interface dependem das versões — manter fixas.)
streamlit==1.50.0
PyMuPDF==1.26.5
shapely==2.0.7
numpy>=1.26,<3
